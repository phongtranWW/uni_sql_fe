import ContentProjectManager from "./content-project-manager";

const ContentTabProject = () => {
  return (
    <div className="flex flex-col h-full">
      <ContentProjectManager />
    </div>
  );
};

export default ContentTabProject;
